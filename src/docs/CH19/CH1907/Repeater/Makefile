# Generated by [Toolkit-Py](https://github.com/fujiawei-dev/toolkit-py) Generator
# Created at 2022-03-08 09:20:05.282409, Version 0.9.3

.PHONY: ;
#.SILENT: ;               # no need for @
.ONESHELL: ;             # recipes execute in same shell
.NOTPARALLEL: ;          # wait for target to finish
.EXPORT_ALL_VARIABLES: ; # send all vars to shell
.IGNORE: clean;            # ignore all errors, keep going

APP_VERSION = 0.0.1
APP_NAME = repeater

UNIFIED_NAME := $(APP_NAME)
ifeq ($(OS), Windows_NT)
UNIFIED_NAME := $(UNIFIED_NAME).exe
endif

CMAKE_BUILD_DIR = cmake-build-release

all: run

build: clean
	cd $(CMAKE_BUILD_DIR); cmake -G "Unix Makefiles" ..; make

run: build
	cd $(CMAKE_BUILD_DIR); ./$(UNIFIED_NAME)

clean:
	-(mkdir $(CMAKE_BUILD_DIR))
	cd $(CMAKE_BUILD_DIR); rm -r *
